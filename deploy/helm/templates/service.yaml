apiVersion: v1
kind: Service
metadata:
  name: {{ include "bws-vault-bridge.fullname" . }}
  labels:
    {{- include "bws-vault-bridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bws-vault-bridge.selectorLabels" . | nindent 4 }}
